interface:
  display_name: "Qlib"
  short_description: "Repo-specific patterns and workflows for qlib"
  default_prompt: "Use the qlib repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true